# AGENTS.md

## Structure

- **Regra do contexto**: sempre que a janela de contexto estiver ficando cheia (risco real de esquecer detalhes importantes deste trabalho), o assistente DEVE me avisar explicitamente e me dizer exatamente o que eu devo falar para ele retomar de onde parou — ou seja, listar os arquivos em andamento, o que falta fazer, comandos de verificação e pontos sensíveis (ex.: não commitar, nomes de endpoints, segredos em uso). Isso é uma regra: avisar é obrigatório, não opcional.
- Two independent apps, **no root `package.json` / workspaces**. Run commands inside `front/` or `back/`; never `npm install` at the repo root.
- `front/` = Angular 20 (standalone), deployed to Vercel.
- `back/` = NestJS 12 (Node ESM) with REST + WebSocket (Socket.io), deployed to Render.
- **No Supabase.** Postgres is **Neon** (serverless) accessed via **Prisma**; auth via **Neon Auth** (Managed Better Auth, beta — users/sessions live in the `neon_auth` schema of the same DB); images go to Cloudflare **R2**; emails via **Resend**; storage and realtime are implemented in the API itself (no RLS on business tables — the API is the only data access layer).
- **DB schema is applied** (M1): `prisma/migrations/20260910114719_m1_core` — see `PLAN.md`.
- Docs/prose (PLAN.md, README) are pt-BR; keep new prose in pt-BR. Code in English.

## Setup gotchas

- Local Node is **22.16.0**. `@angular/cli` is deliberately pinned to **v20** because CLI 22 requires Node >= 22.22.3. Do not upgrade Angular or run `ng update` unless Node is bumped first.
- `back/.npmrc` sets `legacy-peer-deps=true`. **Required**: plain `npm install` crashes on npm 10.9.2 resolving vitest peer deps (`Cannot read properties of null (reading 'edgesOut')`). Never remove this file.
- Back reads env from `back/.env` (copy from `.env.example`): `DATABASE_URL` (Neon DSN, `sslmode=require`), `PORT`, `NEON_AUTH_ISSUER` (issuer/audience do JWT — host `<projeto>.neonauth.sa-east-1.aws.neon.tech`, sem o sufixo `/neondb/auth`), `APP_JWT_SECRET`, `FRONT_URL` (origin pra Neon Auth) e `ADMIN_SISTEMA_EMAIL` (opcional — promove o admin raiz no boot). Neon project not created yet, so `GET /health` returns `degraded` until `.env` is filled.
- Front env lives in `front/src/environments/environment.ts` (`apiUrl`) — points to localhost:3000 in dev.
- Prisma: after changing `back/prisma/schema.prisma`, run `npx prisma migrate dev` (creates/applies migration) — it also regenerates the client. For one-off DB tweaks without a migration, use `npx prisma db push`. Never hand-edit generated client.

## `back/` — NestJS 12

- ESM (`"type": "module"`): relative imports **must** include the `.js` extension (`./app.controller.js`, `../prisma/prisma.service.js`). New files must follow, or build fails.
- Linter is **oxlint**, not eslint: `npm run lint`. Formatter: `npm run format` (prettier).
- Tests via vitest: `npm test` = unit (`**/*.spec.ts`); `npm run test:e2e` = e2e (`**/*.e2e-spec.ts`, separate `vitest.config.e2e.ts`).
- Dev server: `npm run start:dev` (port 3000, reads `PORT`). Build: `npm run build` (nest build).
- Database: `PrismaService` in `src/prisma/` is a `@Global` module — inject it anywhere. `GET /health` runs `SELECT 1` via Prisma. CORS is enabled in `src/main.ts`.
- Auth is **Neon Auth** (Managed Better Auth). **No navegador o SDK NÃO consegue o JWT da Neon**: o `/get-session` manda o JWT EdDSA só no header `set-auth-jwt`, e como a Neon não envia `Access-Control-Expose-Headers` pra esse header, o browser o esconde (SDK `adapter-core-*.mjs` faz `ctx.response.headers.get("set-auth-jwt")` → `null`). O `data.session.token` retornado é o token **opaco** (o cookie é `HttpOnly`, valor `bare.mac`; `neon_auth.session.token` guarda a versão `bare`). Por isso o front chama `POST /auth/exchange` com o token opaco → o back resolve em `neon_auth.session` (`token = <opaco>` ou `LIKE '<opaco>.%'`, `expiresAt > NOW()`, ban ~ `neon_auth."user".banned`) e emite um **JWT HS256 próprio** (`APP_JWT_SECRET`, iss/aud `cardapio-online`, exp 6h) que passa a ser o Bearer. `AuthService.validateSession` tenta o JWT da aplicação primeiro e cai no caminho legado da Neon (verifica assinatura com as chaves de `neon_auth.jwks` — `kid` uuid → cast `${kid}::uuid`; publicKey é JSON `{kty OKP, crv Ed25519, x}`) + `exp`/`iss`/`aud` (= `NEON_AUTH_ISSUER`) + `banned: true` + sessão viva por `userId` (cast `${id}::uuid`). Prisma `$queryRaw` não faz cast implícito de text → uuid — sempre cast. Front: `auth.service.ts` troca o opaco por JWT quando `ehJwt()` falha (token sem 3 segmentos ou payload sem `sub`+`exp`).
- **Admin panel (M2)**: `AdminModule` under `src/admin/` exposes owner-guarded routes (`@UseGuards(AuthGuard)` + `donoId` check, unauthorized = 404): `GET/POST /admin/lanchonetes`, `GET/PATCH/DELETE /admin/lanchonetes/:slug`, `GET /admin/lanchonetes/:slug/cardapio` (incl. inativos + grupos com opções + `grupoIds` por produto), CRUD `categorias`/`produtos`/`grupos`/`opcoes` e `PUT /admin/produtos/:idProd/grupos` (substitui o pivô). Validação manual via helpers do service (sem class-validator); slug `^[a-z0-9]+(?:-[a-z0-9]+)*$` + P2002 → 409. Seed "Sabor da Vila" tem `donoId` fake — `/admin/*` só enxerga lojas do dono logado. **Cada conta pode ter só uma lanchonete**: `POST /admin/lanchonetes` → 400 se o dono já tiver (mensagem "…Cada conta pode ter apenas uma lanchonete."); `admin-home` redireciona direto para `/admin/:slug/config` quando já existe. Lanchonete com pedidos não pode ser excluída (400) — checagem por `pedido.count` (ver gotcha do FK no fim desta seção).
- **Onboarding (M6)**: `Lanchonete.tipo String?` (`m3_lanchonete_tipo`): `lanches|pizzaria|sorvetes|acai`. `create()` em `src/admin/admin.service.ts` aceita **só `nome`** (slug automático via `gerarSlug`: lowercase → NFD strip diacritics → não-alnum vira `-` → colapso → trim → slice 60); nome 2..50 chars, regex `/^[\p{L}\p{N} ]+$/u`, msg de símbolos: "O nome só pode conter letras, números e espaços". Slug duplicado → **409 sem sufixo** (o usuário deve trocar o nome): "Já existe uma lanchonete com esse nome (endereço /<slug>). Escolha outro nome." `updateConfig` valida `tipo`/`nome`. 4 **logos padrão** SVG servidas por `src/assets/assets.controller.ts` em `GET /assets/logo-:tipo.svg` (image/svg+xml, cache 86400): lanches 🍔 #e8800c, pizzaria 🍕 #d64541, sorvetes 🍨 #f2a2c5, acai 🍧 #6b3fa0. Front: `front/src/app/components/header-lanchonete.component.ts` (header reutilizável logo+nome+fonte+cor, tamanhos md/lg — usado em admin-cardapio, admin-config, cardápio público e onboarding), `front/src/app/services/lanchonete-visual.ts` (`TIPOS_LANCHONETE`, `FONTES_PADRAO`, `logoPadrao(tipo)` → `${apiUrl}/assets/logo-<tipo>.svg`), página `/onboarding` com wand 5 passos (nome→tipo/logo→contato/endereço→cor/fonte com preview→finalizar; rota com `authGuard`), e redirect pós-login/cadastro: sem lanchonetes → `/onboarding`, senão `/admin` (honra `?redirect=`). `admin-home` cria só com nome; `admin-config` ganhou select de tipo/fonte + botão "Padrão" (aplica logo do tipo) + preview do header.
- **Painel da plataforma / super-admin**: `AdminSistemaModule` em `src/admin-sistema/` — tabela `admin_sistema` (`id` = id do usuário neon_auth, `funcao`: `super` raiz | `admin` humano; `email` único; migrações `m4_admin_sistema`/`m4b_admin_sistema_email_unico`). Guard `AdminSistemaGuard` (usar sempre junto: `@UseGuards(AuthGuard, AdminSistemaGuard)`) exige registro na tabela senão 403. Rotas (Etapa 1/M5'): `GET /admin-sistema/lanchonetes` (todas as lojas + `situacao`, `donoNome`/`donoEmail` via join `neon_auth."user"` + contagens categorias/produtos/pedidos), **consulta read-only** `GET /admin-sistema/lanchonetes/:id` (dados), `/:id/cardapio` (inclui inativos + grupos/opções) e `/:id/pedidos`, `PATCH /admin-sistema/lanchonetes/:id/situacao` (valores de `SITUACOES_LANCHONETE` na service; 400 se inválida ou igual à atual; registra log de ação), `DELETE /admin-sistema/lanchonetes/:id` (somente `funcao: super` senão 403; 400 se tiver pedidos via `pedido.count`; registra log; ver gotcha do FK), `GET /admin-sistema/logs?nivel=&categoria=&q=` (filtros + join do `usuarioNome`/`usuarioEmail`), `GET/POST/DELETE /admin-sistema/admins` (POST cria conta humana via Neon Auth sign-up server-side e loga; DELETE bloqueia `super` e o próprio). **Primeiro admin**: `npm run criar:admin -- <email> <senha> <nome>`; alternativa: `ADMIN_SISTEMA_EMAIL` no boot (upsert `funcao: super`). Env: `FRONT_URL` (origin nas chamadas à Neon Auth, default localhost:4200). `/me` retorna `adminSistema` **e** `adminSistemaFuncao`. **Gotcha Neon Auth cru (fetch, sem SDK)**: sucesso = `{ token, user }` top-level e erro = `{ message, code }`. Front: rota `/painel-admin` (`pages/admin/painel-admin.component.ts`) — abas Lanchonetes (aprovador/pausar + consulta de dados/cardápio/pedidos), Logs (filtros) e Administradores (criar/remover); exclusão só disponível p/ `super`. Redireciona super-admin pós-login/cadastro.
- **Situação da lanchonete + logs (Etapa 1/M5')**: `Lanchonete.ativa` foi substituída por `Lanchonete.situacao String @default("pendente")` (`pendente|ativa|pausada`; migração `20260919120000_m5_lanchonete_situacao_logs` com backfill de `ativa`); novo cadastro (onboarding/`admin-home`) nasce **pendente**. Gating público (`GET /l/:slug/cardapio` em `lojas.service.ts`) e pedidos (`pedidos.service.ts`) exigem `situacao: 'ativa'`. O painel do dono (`admin.service.ts`) **não** gerencia mais situação — `CONFIG_FIELDS` não inclui `ativa`/`situacao`, mas `toConfig`/`listMine` ainda expõem `situacao` (+ `ativa` derivado) para não regredir o front do dono. Modelo novo `LogSistema` (mapa `logs_sistema`): `nivel` (`info|aviso|erro`), `categoria` (`acao|erro`), `mensagem`, `detalhes JSONB`, `usuarioId` (uuid), `createdAt`; helper `registrarLog` na `AdminSistemaService` + `registrarErro`. **5xx são logados automaticamente** pelo `ErroLogFilter` global (`src/common/erro-log.filter.ts`, registrado em `main.ts` com `app.get(PrismaService)`). Seed de teste: `npm run seed:teste` (`scripts/seed-teste.mjs`) — cria donos+lojas (Duarte Burguers ativa, Cantina da Nonna pendente) com cardápio e a conta fixa de cliente (`cliente.teste@teste.dev` / `Teste1234`); idempotente por e-mail/slug (reusa usuários existentes; o criar direto na Neon com e-mail duplicado daria erro). **Status dos pedidos (Etapa 4)**: cadeia `recebido→em_preparo→enviado→entregue→finalizado` + `cancelado` (só antes de sair pra entrega — `recebido|em_preparo` — com **justificativa obrigatória**). `PATCH /admin/pedidos/:idPedido/status` valida transição por etapa (`TRANSICOES_STATUS` em `admin.service.ts`; pulo/volta/same-status → 400) e grava `justificativaCancelamento` (migração `m8_pedido_status_justificativa`, coluna `TEXT` nullable em `pedidos`). Realtime `pedido:status` mantém payload `{id,numero,status}`. **Etapa 2 (dono pendente + diretório)**: `GET /l` público (`src/lojas/diretorio.controller.ts`, `situacao: 'ativa'`, orderBy nome) — **home não é mais landing/diretório**: `/` virou tela de login (mesma `AuthComponent` do `/auth`), então essa rota é pública no back mas sem entrada no front por enquanto; quem tem lanchonete `pendente` edita config+cardápio normalmente mas a aba Pedidos mostra "loja indisponível" (front checa `config.situacao`); `slug` editável via `PATCH /admin/lanchonetes/:slug` (slugs em `CONFIG_FIELDS`, P2002 → 409) + botão copiar link no `admin-config`; front `LanchoneteResumo`/`LanchoneteConfig` expõem `situacao` (back `listMine` não manda mais `ativa`).
- **Pedidos (M3)**: `PedidosModule` under `src/pedidos/` — `POST /l/:slug/pedidos` (autenticado): valida itens qtd 1..99 e opções reais e ativas do produto, aplica regras do grupo (`unica`, `obrigatorio`, `minSelecoes`/`maxSelecoes`, "remover" não combina com acréscimos), registra snapshot em `pedido_itens`/`pedido_item_opcoes`, número sequencial por lanchonete (`MAX(numero)+1`), exige `chavePix` configurada (senão 400), estampa `brCodePix` gerado pelo util `src/pedidos/pix.ts` (BR Code EMV com CRC16-CCITT `0x1021`, txid `***`, GUI `br.gov.bcb.pix`). **Forma de pagamento**: aceita `formaPagamento?: 'pix'|'cartao'|'dinheiro'` no body (default `pix`; inválida → 400 "Forma de pagamento inválida."; validador `validaFormaPagamento`); **`brCodePix` só é gerado/gravado se a forma for `pix`** (senão fica `null`). Endereço por `enderecoId` (do dono) ou inline `endereco` (snapshot em `enderecoEntrega`). `MeService` ganhou auto-criação do `clientes` (id = user do Neon Auth) + CRUD `GET/POST /me/enderecos`, `PATCH/DELETE /me/enderecos/:id` (padrao único) + `GET /me/pedidos` (histórico com itens/opções/endereço).
- **Painel realtime (M4)**: `PedidosGateway` (`src/pedidos/pedidos.gateway.ts`, Socket.io) registrado em `PedidosModule` (providers + exports) e injetado em `AdminService` via `AdminModule` (que importa `PedidosModule`). `handleConnection` valida o JWT de forma assíncrona e guarda a promise em `client.data.authPromise`; handlers como `loja:join` devem `await client.data.authPromise` (nunca ler `client.data.userId` direto — eventos chegam antes da validação terminar). Canais: `loja:join`/`loja:join:ok`/`erro`, `pedido:novo` (na criação), `pedido:status` (na mudança). Rotas do painel (dono-check 404): `GET /admin/lanchonetes/:slug/pedidos?status=` e `PATCH /admin/pedidos/:idPedido/status` com `PEDIDO_STATUSES` (`recebido→em_preparo→enviado→entregue→finalizado|cancelado`; ver Etapa 4 na seção acima). Front: `front/src/app/services/pedidos-realtime.service.ts` (client Socket.io com JWT), `front/src/app/services/pedido-painel.ts` (tipos/labels pt-BR) e `front/src/app/pages/admin/admin-pedidos.component.ts` (aba "Pedidos" no shell, rota `pedidos`).
- **Imagens / galeria (M5)**: `R2Module` global em `src/r2/` (`R2Service` com `@aws-sdk/client-s3` — endpoint `https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com`, `upload()`/`publicUrl()`/`configured`; **adormecido por enquanto** — sem env → não configurado e `upload` lança; o seed NÃO usa R2, grava URL do Pexels, upgrade futuro é só gravar a URL do R2 no campo). `GaleriaModule` em `src/galeria/`: público `GET /galeria/categorias` (slugs fixos em `GALERIA_CATEGORIAS`) e `GET /galeria?q=&categoria=` (filtro por `keywords` com `has` — token exato minúsculo — e categoria válida senão 400, `take 60`). Model `GaleriaImagem` (migração `m2_galeria`). Seed da galeria: `npm run seed:galeria` (`scripts/seed-galeria.mjs`, roda `node --env-file=.env`) consulta o Pexels (`PEXELS_API_KEY`, curadoria: hamburguer 3, batata 4, sucos 3, sorvetes 3, acai 1, pizza 3, refrigerantes 3, cerveja 2 — 22 imagens) e grava `foto.src.large2x` em `url` (skip idempotente por `url` única). Front: `front/src/app/services/galeria.service.ts` + picker overlay no `admin-cardapio` (categoria/produto definem `imagemUrl` via CRUD que já aceita o campo; buscar limpa para nulo).
- **Gotcha FK (Neon/pooler)**: tentar `lanchonete.delete()` com pedidos **não** vira `P2003` — na Neon (com `channel_binding`/pooler) a violação RESTRICT chega como `PrismaClientUnknownRequestError` com SQLSTATE `23001` (code Prisma `undefined`), então `instanceof Prisma.PrismaClientKnownRequestError` falha e vira 500. Por isso os deletes de lanchonete checam `pedido.count` **antes** e só então chamam `delete` (evita depender do código de erro).
- Future modules (M1+): Resend emails. Neon Auth integration is **done** (front `@neondatabase/neon-js` + back JWT validation above).
- **Cliente (Etapa 3)**: fluxo do consumidor. `PATCH /me` (perfil: `nome`/`telefone`) — telefone normalizado (só dígitos 10..13; valor não-dígito → 400; `""`/`null` limpa) e validação manual (sem class-validator). `POST /l/:slug/pedidos` aceita `telefone` opcional: valida e arquiva snapshot em `Pedido.clienteTelefone` (migração `m6_cliente_telefone`) e atualiza `clientes` do usuário (`ensureCliente` agora leva telefone); sem `telefone` o snapshot fica nulo e o cliente não é sobrescrito. JWT da app passou de `6h` para `7d` (`exchangeSessionToken`). `GET /me/pedidos` agora expõe `produtoId` (por item) e `opcaoId` (por opção) — usado pelo "Pedir de novo". Front: checkout (`cardapio.component.ts`) mostra campo **"Seu WhatsApp"** quando `/me` retorna `cliente.telefone` vazio (valida `^\d{10,13}$` antes de finalizar; envia no pedido e libera o campo ao salvar); página `meus-pedidos` ganhou bloco PIX (QR + copia-cola) para status `recebido|em_preparo|enviado` (antes de entregue; set compartilhado `STATUS_COM_PIX` no front em `pedido-painel.ts`), link da loja e botão "Pedir de novo" (`repetirPedido` recria carrinho por slug — desabilitado se algum item perdeu `produtoId`, ex. produto excluído com `ON DELETE SET NULL`); login com **Google** na tela de auth (`auth.service.signInGoogle` → `client.signIn.social({ provider: 'google', callbackURL: origin + '/auth' })`) — em dev a Neon usa usuários compartilhados; **home = login**: `/` e `/auth` renderizam a mesma `AuthComponent` (com branding), e o auto-redirecionamento de quem já está logado só acontece com `?redirect=` ou no retorno do OAuth (marca `auth.oauth-pendente` em `sessionStorage` definida em `entrarComGoogle`) — sem isso a tela mostra a conta logada com botão **Sair**, permitindo trocar de conta sem cair numa sessão antiga (bug em que tentar logar como Duarte abria a conta da Nonna). Rotas protegidas por `authGuard` (`minhas-pedidos` agora está incluso) preservam a URL como `?redirect=`. **Sacola/pedido persistente**: após confirmar o pedido o carrinho é limpo, mas `pedido()` fica na memória da página — o modal PIX é controlado por `pedidoPainelAberto` (fechar só esconde); com o carrinho vazio, uma `barra-pedido` fixa (Pedido #N · status · total) reabre o modal (QR/copiar, escondido se o pedido saiu de `recebido|em_preparo|saiu_para_entrega`)<br>
**QR PIX só para PIX**: `meus-pedidos` e a barra/modal de cobrança checam `pedido.formaPagamento === 'pix'` além de `brCodePix`/status (`mostraPix()`); o back só grava `brCodePix` quando `formaPagamento: 'pix'` (ver seção Pedidos). O rodapé do pedido (`rotuloPagamento`) mostra a forma real (PIX/Cartão/Dinheiro) em vez do span fixo. **Gotcha servidor**: o back local roda `dist/main` compilado (chain `npm start` → `nest start` → `node dist/main`) — mudanças no back exigem `npm run build` **e reinício do processo** (não é `start:dev` com watch); o front usa `ng serve` (PID próprio). e tem atalho para `/minhas-pedidos`; status do pedido é revalidado a cada 20s via `GET /me/pedidos` e ao reabrir (`reabrirPedido`/`refreshPedidoStatus`); estado do pedido zera ao trocar de slug.

## `front/` — Angular 20

- Root component is `src/app/app.ts` (Angular 20 scaffold convention; template `app.html`, styles `app.scss`) — not `app.component.ts`.
- Commands: `npm start` (dev), `npm run build` (output `dist/front`), `npm test` (karma/jasmine — needs Chrome/ChromeHeadless).
- Styles are SCSS. Prettier config lives inside `front/package.json` (printWidth 100, singleQuote).
- API calls go through Angular `HttpClient` toward `environment.apiUrl` (auth: JWT Bearer; no Supabase client anymore).
- Neon Auth lives in `front/src/environments/environment.ts` as `neonAuthUrl` (`https://<projeto>.neonauth.sa-east-1.aws.neon.tech/neondb/auth`) and is wrapped by `src/app/services/auth.service.ts` (`createAuthClient` from `@neondatabase/neon-js/auth`). The JWT (via `POST /auth/exchange`) is sent to the API as Bearer by the functional interceptor `auth.interceptor.ts` (auto-refresh via `getSession()` + novo exchange when ~1 min from `exp`, plus one retry on 401). Note: the compute host from `DATABASE_URL` does NOT serve `/auth` — the console gives the real auth base URL.
- **Smokes multi-usuário na prática**: o JWT **não** vem do corpo do `POST /sign-up/email`/`GET /get-session` (o `session.token` retornado é o valor **opaco**/bare do cookie); em Node o SDK de `@neondatabase/auth` lê o JWT do header de resposta **`set-auth-jwt`** do `/get-session` (`dist/adapter-core-*.mjs`: `ctx.response.headers.get("set-auth-jwt")`); no browser esse header fica oculto (CORS, sem `Access-Control-Expose-Headers`) — usar `POST /auth/exchange`. Para montar smokes com N usuários distintos rode fetch cru por usuário (cookie próprio + capturar `set-auth-jwt` no Node) — instâncias do SDK no mesmo processo compartilham cookie jar em memória (2º cliente retorna sessão do 1º).

## Handoff — home do cliente + lanchonetes favoritas (Etapa 3, concluído)

**Login**: `npm run seed:teste` (idempotente) garante as contas no Neon Auth — `cliente.teste@teste.dev / Teste1234`, `lojista.duarte@teste.dev / Teste1234`, `lojista.nonna@teste.dev / Teste1234`, `admin@cardapio.dev`. O "invalid" era a conta do cliente não existir no `neon_auth."user"` (Neon devolve `INVALID_EMAIL_OR_PASSWORD` tanto pra e-mail inexistente quanto pra senha errada). **Login do cliente agora funciona.**

**Bug corrigido**: `posLogin` em `front/src/app/pages/auth/auth.component.ts` não manda mais cliente pro `/onboarding` — usuário sem lanchonetes vai pra `/home` (admin-sistema → `/painel-admin`, dono com lanchonete → `/admin`). Quem quiser abrir loja usa o link "Sou lojista" (`/admin`) na home do cliente.

**Feature implementada — home do cliente + lanchonetes favoritas:**
- **Back**: tabela `lanchonetes_favoritas` (migração `20260920163718_m7_lanchonetes_favoritas`): `clienteId` (uuid = Neon Auth) + `lanchoneteId` (FK cascade) + `createdAt`, `@@unique([clienteId, lanchoneteId])`. `FavoritasModule` em `src/favoritas/` (registrado no AppModule): `GET /favoritas` (autenticado, só `situacao: 'ativa'`, orderBy `createdAt desc`, inclui dados da lanchonete), `PUT /favoritas/:lanchoneteId` (upsert `update: {}` — não reordena; 404 se não ativa) e `DELETE /favoritas/:lanchoneteId`. **Auto-favorita na 1ª finalização**: em `pedidos.service.ts` `criaPedido`, após o pedido, `lanchoneteFavorita.upsert` silencioso (try/catch + warn) — o cliente que finaliza um pedido ganha a loja nas favoritas sem prompt.
- **Front**: rota `/home` (`HomeComponent` em `pages/home/`, `authGuard`, registrada antes do catch-all `:slug`) — header com links Meus pedidos / Sou lojista / Sair; cards "Meus dados" (nome + WhatsApp via `PATCH /me`) e "Meus endereços" (CRUD `/me/enderecos`: novo/salvar, remover com `confirm`, definir padrão). Lista mostra **apenas favoritas** (ordem decrescente de favoritação) com **filtro por nome, endereço e tipo** (buscando no diretório público `GET /l` também exibe não-favoritas para favoritar/desfavoritar via botão ♥ por linha); cada item mostra **itens na sacola** daquela lanchonete (lê `localStorage cart.<slug>` via `CartService.totalItensNoSlug`). Serviço `favoritas.service.ts`. **Botão "Minhas lanchonetes" no topo do cardápio** (`CardapioComponent`, `logado()` ligado ao auth) leva pra `/home`; "Voltar ao início" em `meus-pedidos` agora aponta pra `/home`.
- **Próximos passos em aberto (se houver)**: apenas seguir o plano geral em `PLAN.md`.

## Handoff — identidade da home + e-mail de contato para lojistas (concluído)
- **Back**: tabela `config_plataforma` (linha única `id='global'`, coluna `emailLojista Text?`; migração `20260920171854_m9_plataforma_email_lojista`). `AdminSistemaService` ganhou `getPlataforma()` e `updatePlataforma(user, body)` (validação manual: corpo sem/`''`/`null` limpa; e-mail inválido → 400 "E-mail inválido."; upsert na linha global + `registrarLog` "E-mail de contato atualizado"). No boot, se a config não tiver e-mail, `configurarEmailPadrao` grava o e-mail do primeiro `funcao: super` (default = e-mail do admin raiz). Rotas guardadas no `AdminSistemaController` (AuthGuard + AdminSistemaGuard): `GET /admin-sistema/plataforma` e `PATCH /admin-sistema/plataforma`. **Endpoint público** `GET /plataforma/email-lojista` → `{ email }` em `src/plataforma/` (`PlataformaModule`, registrado no AppModule) — é o que a home do cliente consome.
- **Front**: home do cliente (`home.component`) trocou o título "Minhas lanchonetes" por **"Página principal"**, ganhou o subtítulo **"Lanchonetes favoritas"** antes do filtro e uma seção final "Quer vender pelo Cardápio Online?" com `mailto` pro e-mail configurado (rota pública `GET /plataforma/email-lojista`). No header, o link "Sou lojista" virou **"Quero vender"** (mailto) quando há e-mail configurado, senão cai pro `/admin` como fallback. Botão no topo do cardápio "Minhas lanchonetes" → **"Página principal"**. Painel admin (`/painel-admin`, aba **Configurações**): campo editável do e-mail de contato (`novoEmailLojista`, `emailValido`, `salvarEmailLojista` → `PATCH /admin-sistema/plataforma`; `PainelAdminService.getPlataforma/atualizarPlataforma`). **Gotcha**: nunca rodar `prettier --parser html` em arquivos `.ts` — o parser HTML remove `<` de genéricos, quebra crases/acentos e corrompe o arquivo irrecuperavelmente (use `edit`+`prettier` sem `--parser` global).

## Handoff — login: olhinho, lembrar de mim e esqueci senha (concluído)

- **Back**: novo endpoint público `POST /auth/verificar-email` → `{ email }` → `{ registrado: boolean }` (`AuthService.verificarEmailExistente`, `$queryRaw` em `neon_auth."user"` com `LOWER(email) = LOWER(${email})`; e-mail fora do regex `EMAIL_REGEX` → 400 "E-mail inválido."). Só existe para o front confirmar que o e-mail é cadastrado **antes** de disparar o e-mail. Testes em `src/auth/auth.service.spec.ts` (4 casos).
- **Front** (`auth.component` + `auth.service`): tela de auth agora tem 3 "telas" (signal `tela`): `login` | `recuperar` | `redefinir`. **Olhinho** nas senhas (`mostrandoSenha`/`mostrandoNovaSenha` + botão com `pi-eye`/`pi-eye-slash` dentro do wrapper `.senha`). **Lembrar de mim** (`lembrar()` — checkbox nativo) salva `auth.lembrar` + `auth.email-salvo` no localStorage e passa `rememberMe` no `signIn.email`. **Esqueci senha** (`abrirRecuperacao`): `recuperar` pede o e-mail → `AuthService.verificarEmailCadastrado` (se não cadastrado: "E-mail não cadastrado na plataforma.") → via SDK `client.requestPasswordReset({ email, redirectTo: origin + '/auth?esqueci=1' })` → volta ao login com `sucesso` "Enviamos um link...". **Redefinir**: o link do e-mail cai em `/auth?esqueci=1&token=…` → `tela = 'redefinir'` com "Nova senha" + "Confirmar nova senha" → `client.resetPassword({ newPassword, token })` → sucesso volta ao login ("Senha redefinida..."). Token inválido/expirado → mensagem para solicitar novo.
- **Fluxo do reset na Neon (verificado em smoke real)**: `POST {authBase}/request-password-reset` exige header `Origin` (o navegador manda sozinho; em fetch cru precisa passar — sem ele dá 403 INVALID_REDIRECT_URL mesmo com a origem confiável). O token fica em `neon_auth.verification` (`identifier = 'reset-password:<token>'`, `value` = userId, expira 1h). O e-mail contém `${authBase}/reset-password/<token>?callbackURL=<redirectTo encodado>`; esse GET faz 302 para `redirectTo?token=<token>`. `POST {authBase}/reset-password` `{newPassword, token}` **também exige Origin header** e sobrescreve a senha (senha < 8 → PASSWORD_TOO_SHORT; token inválido → INVALID_TOKEN; se o usuário só tinha login social, cria a conta `credential`). **Entrega do e-mail depende da config de e-mail da Neon no console** (ainda em aberto — Banco de emails/Resend); o fluxo de link+reset já está 100% validado (troquei a senha do `cliente.teste@teste.dev` e reverti para `Teste1234`).

## Handoff — marca Peditto + app bar (logo + dark mode) (concluído)

- **Nome do sistema virou "Peditto"** (antes "Cardápio Online"/"PediJá"). Imagens em `front/public/`: `logo.png` (app bar) e `logo-sf.png` (login). Título do `index.html` = "Peditto".
- **Login** (`auth.component`): o `header.marca` com h1+slogan (h1 era `PediJá`) virou `<img src="logo-sf.png">` (`marca__logo`, max-width 250px, centralizada). Removi `nomePlataforma`/`slogan` do ts e o estilo `.marca__apoio` do scss.
- **App bar global** (`app.ts`/`app.html`/`app.scss`): signal `temaGlobal` virou **`appBar`** — calculado no `NavigationEnd` por `urlAfterRedirects`: mostra em **todas as rotas exceto login** (`''`, `/`, `/auth…`). Header `<header class="app-bar">` com `logo.png` (alt "Peditto") à esquerda + `<app-tema-switch />` à direita; `position: sticky; top: 0; z-index: 900`. O fundo do app bar é **sempre escuro** (`background: var(--p-surface-950)`, independente do tema — usuário pediu header escuro mesmo no light mode) e `border-bottom: 1px solid var(--p-surface-700)` (linha separadora). Valor inicial `true` (evita flash; login rapidamente vira `false` no primeiro evento de navegação). `logo.png` tem fundo transparente (verificado). **`tema-switch.component.ts`** é um botão único (`p-button`, `[rounded] [text]`) com **lua** `pi-moon` no light (clica → dark) e **sol** `pi-sun` no dark (clica → light), `tema.definir(!tema.escuro())`.
- **Toggle flutuante removido**: o `<app-tema-switch class="app-chrome" />` do `app.html` e o bloco `.app-chrome` de `styles.scss` foram apagados — o dark mode agora vive só no app bar (inclusive no cardápio público e na home). Removeu-se também o `app-tema-switch` do topo da home (`home.component`, import+uso); `.home__links` ficou só com "Meus pedidos" + "Sair". Texto "Quer vender pelo Cardápio Online?" virou "Quer vender pelo Peditto?".

## Handoff — aba Cardápio com modais + validação por campo (concluído)

- **Front** (`front/src/app/pages/admin/admin-cardapio.component.ts`): adicionar/editar **categoria**, **grupo de opções**, **produto** e **opção** agora abrem **modais** (PrimeNG `p-dialog`); não há mais formulários inline de criação/edição (só botões Editar/Adicionar abrindo modal). Estado por modal: `catModal`/`grupoModal`/`prodModal`/`opcaoModal` (objetos) + `*Erros: Record<string,string>` (`salvando` compartilhado). **Validação client-side** no submit (`validaNome`, preço ≥ 0 e inteiros para min/max); campo inválido recebe classe `.erro` (borda vermelha + `box-shadow` inset) e mensagem `.erro-txt` logo abaixo; erros do backend/gerais caem em `*Erros['geral']` (banner vermelho no rodapé do modal). Mensagens pt-BR.
- **Produto**: criar/editar envia também `descricao`/`imagemUrl`/`destaque`/`ativo` (o back já aceitava) + `setProdutoGrupos` (grupos idem para criação — produto novo já sai com grupos marcados) e **recarrega** após salvar (mantido). `abrirProdEditar(catId, produto)` recebe o `cat.id` (o guard de `categoriaId` bloquearia a edição sem isso). Galeria: `appendTo="body"` para abrir por cima dos modais. Hotfix TS: `maxSelecoes` é `number | null` — comparar com `''` dá TS2367 (input number escreve `null` quando vazio).
- **Admin shell** (`admin-shell.component.ts`): topo do perfil da lanchonete agora tem a **logo escolhida à esquerda** do nome (signal `logo`, lido de `config.logoUrl`; `object-fit: contain`, max 3.5rem), "Ver página" + "Sair" **alinhados à direita** (`.shell__acoes { margin-left: auto }`, removido o `padding-right: 7.5rem` antigo) e as **abas centralizadas** (`.shell__abas` com `margin: 18px auto 22px`).
- **Forma de pagamento no checkout** (`cardapio.component.ts`/`.html`): modal "Finalizar pedido" ganhou seção **Forma de pagamento** — 3 checkboxes exclusivos (Pix vem marcado por padrão; trocar desmarca os outros via `definirFormaPagamento`): **Pix | Cartão de crédito/débito | Dinheiro**. Se Dinheiro: checkbox "Precisa de troco?" + campo "Troco para quanto? (R$)" (validação: número > 0 → senão erro). A forma escolhida vira **prefixo da observação** do pedido (`observacaoMontada()`: `Forma de pagamento: …\n` + obs do cliente; Dinheiro com troco → `Forma de pagamento: Dinheiro (troco para R$ X.XX)`) — back guarda junto (campo `observacao`). Após confirmar, o sinal `formaPedido` guarda a forma escolhida e `precisaPagar()` só retorna true para PIX (modais/barra-pedido de cobrança não aparecem para Cartão/Dinheiro).
- **admin-config (Dados cadastrados)**: campo do slug renomeado para **"Rota de acesso (slug)"** com tooltip (**dinâmico**, `tooltipSlug(c.slug)` via `[attr.title]` + `pi-info-circle`, classe `.rotulo-com-dica`) — o tooltip explica minúsculas/hífens/editável **e já mostra o link atual** (`/{{slug}}`), substituindo a linha de texto que ficava no form (removida para não quebrar o alinhamento logo×slug no mobile). **Alinhamento logo×slug**: os inputs de slug/logo ficam com `flex: 1` (preenchem a coluna) e os botões "Copiar link"/"Padrão" mantêm o **tamanho natural** do conteúdo (sem largura fixa forçada; `p-button button { white-space: nowrap }`). Feedback de "Copiar link": usa `mensagem` + getter `isSucesso` (verde) no aviso do rodapé, com timeout 2,5s. **Notificações de pedidos**: `.notifs` é `grid repeat(3, minmax(0,1fr))` — os 3 toggles (Painel/WhatsApp/E-mail) ficam sempre na mesma linha (no mobile também, com fonte/gap menores). **Botões Salvar/Excluir**: `.acoes` com `justify-content: flex-end` (alinhados à direita).

## Handoff — painel da plataforma: topo, badges, abas, consulta e administradores (concluído)

- **Back** (`AdminSistemaService`/`AdminSistemaController`): nova rota **`PATCH /admin-sistema/admins/:id`** → `atualizarAdmin(id, body, user)` — valida nome (regex de letras/números/espaços), **rejeita nome igual ao atual** (400 "Nenhuma alteração..."), atualiza `admin_sistema.nome` e registra log **"Administrador editado"** com `{ adminId, adminEmail, nomeAnterior, nomeNovo }`. Já existiam logs de criação/remoção. Testes novos em `admin-sistema.service.spec.ts` (4 casos — total 34 no back).
- **Front** (`painel-admin.component.ts`; service `PainelAdminService.atualizarAdmin`):
  - **Topo**: botão "Início" removido; `<p-tag>` agora mostra **"Raiz"/"Admin"** (maiúsculo, sempre visível — warn p/ raiz, secondary p/ admin); novo botão **"Sair"** (`pi-sign-out`, `.pa__sair { margin-left: auto }`) alinhado à direita do título (`.pa__topo` com `align-items: center`, sem `padding-right` fixo). `sair()` = `AuthService.signOut()` + navigate `/`.
  - **Badges maiúsculos**: `SITUACAO_LABELS` virou `Pendente/Ativa/Pausada` (`rotuloSituacao`); `rotuloTipo(tipo)` usa `TIPOS_LANCHONETE.rotulo` (senão capitaliza o valor cru) — usado na consulta; funções dos admins ("Raiz"/"Admin"), `nivel`/`categoria` dos logs via `capitalizar()`; tipo da lanchonete na coluna Status já passa por `rotuloSituacao`.
  - **Fix hover da aba ativa**: `.pa__aba--ativa` ganhou `color: ... !important` (padrão do `admin-shell`) — antes o `:hover` (`color: primary`) sobrepunha a classe ativa e o texto sumia sobre o fundo primary.
  - **Trocar de aba fecha a consulta**: todos os botões de aba usam `trocarAba()` (que chama `fecharConsulta()` antes de trocar), então sair da consulta de uma lanchonete mostra o conteúdo da nova aba.
  - **Administradores**: form inline de criação removido → `p-dialog` **"Criar administrador"** (botão no topo, visível só pro raiz; campos nome/e-mail/senha + `adminFormValido`); cada linha ganhou botão **"Editar"** (`p-dialog` com só o nome, `editarAdminNome`/`salvandoAdminEdicao`/`erroEditarAdmin`). Criar/Editar/Remover só aparecem para `eSuper()`. Erros dos modais via `*AdminErros`/`pa__erro`.
  - **Filtros na aba Lanchonetes**: `.pa__filtros` com **busca** por nome//slug/dono (`filtroBusca` → `lanchonetesFiltradas()` computed) + **Situação** e **Tipo** (selects com `situacaoFiltros`/`tiposFiltros` — arrays declarados como const de módulo **e** expostos como field do componente, pois template Angular não enxerga const de módulo). Empty state "Nenhuma lanchonete encontrada." quando o filtro zera a lista. CSS: `.pa__filtro-busca { flex: 1; min-width: 16rem }` (100% no mobile), `.pa__bloco-topo` (título + botão), `.pa__modal-corpo`/`.pa__modal-acoes`/`.pa__modal-info`.
  - Nota: construção do painel BH `carregando()` inicial `true` engloba todas as abas; logs `filtrosNivel`/`filtrosCategoria` já estavam. `dist-back.out.log` reflete o boot com a nova rota mapeada.

## Handoff — login/cadastro: forms separados, tipo de conta e e-mail único (concluído)

- **Login × Criar conta viram forms separados** (`auth.component.ts`/`.html`): no modo `Entrar` ficam os campos `email`/`password` + **Lembrar de mim** e **Esqueci senha** (`.form__linha` só renderiza `@if (mode() === 'login')`). No modo `Criar conta` há signals próprios (`cadastroNome`/`cadastroEmail`/`cadastroSenha`) — `setMode('cadastro')` **limpa esses campos** a cada troca (o e-mail do login não vaza pro cadastro). Sinal antigo `name` removido.
- **Tipo de conta no cadastro**: `tipoConta` (`cliente`|`lojista`) via `p-selectbutton` (`tipoContas` = "Sou cliente"/"Quero vender") com dica dinâmica. No submit: se **lojista**, grava `auth.modo-cadastro='lojista'` no `sessionStorage`; `posLogin` consome e apaga — sem lanchonetes → **lojista vai pra `/onboarding`** (wand), **cliente vai pra `/home`** (`?redirect=` tem prioridade; adminSistema → `/painel-admin`; quem já tem lanchonete → `/admin`).
- **E-mail único**: Neon Auth já garante unicidade (sign-up rejeita duplicado); o front mapeia a mensagem do SDK para **"Já existe uma conta com esse e-mail."** (`mensagemDeErro` — regex `already exists|already register|já existe|in use|em uso|earlier` só no modo cadastro).
- **Flash do fluxo de criação corrigido** (`admin-home.component.ts`): o form "Criar lanchonete" agora renderiza **só** `@if (!carregando && lanchonetes().length === 0)` — antes aparecia enquanto o `listMine()` carregava e o dono com lanchonete via o form piscar antes do redirect pra `/admin/:slug/config`.
- Validação: build do front OK (`dist/front`). Sem mudança no back.